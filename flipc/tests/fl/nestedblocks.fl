main() {
    let foo = 1;
    while 1 >= 1 { 
        let foo = 400;
        if foo < 401 {
            while 1 == 1 {
                let bap = foo;
            };
        };
    };

    a();
}
 
a() {
    foo = "hi";
    let baz = 0;
    # let baz = 1;
    # let bar = baz * 3 + 2;
    if baz > 0 {
        let bap = "foo is positive!";
        baz = bap;
    };
}

